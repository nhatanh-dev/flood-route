TramYTe - Unity handoff package

Primary Unity import:
- TramYTe_Unity.fbx

Optional files:
- TramYTe_Unity.glb: portable backup/import reference.
- TramYTe.blend: Blender source file for future edits.

Notes:
- The FBX export contains mesh objects only, without QA cameras or lights.
- Materials are exported as simple mesh/material data for Unity import.
- No external texture files are required.
- The corrected sign text is: "TRẠM Y TẾ XÃ TRUNG HOÀ".
- Do not use TramYTe.blend@ if it appears in the source folder; it is a Blender temporary/backup artifact and is not included in this package.
